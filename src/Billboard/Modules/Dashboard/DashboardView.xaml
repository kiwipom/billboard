<common:LayoutAwarePage
    x:Name="pageRoot"
    x:Class="Billboard.Modules.Dashboard.DashboardView"
    d:DataContext="{d:DesignInstance Type=Dashboard:DesignDashboardViewModel, IsDesignTimeCreatable=True}"
    IsTabStop="false"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:common="using:Billboard.Common"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:Dashboard="using:Billboard.Modules.Dashboard" mc:Ignorable="d">

    <common:LayoutAwarePage.Resources>
    	<common:IntToVisibilityConverter x:Key="IntToVisibilityConverter"/>
        <CollectionViewSource
            x:Name="groupedItemsViewSource"
            Source="{Binding Buckets}"
            IsSourceGrouped="false"
            d:Source="{Binding Buckets, Source={d:DesignInstance IsDesignTimeCreatable=True, Type=Dashboard:DesignDashboardViewModel}}" />
        <DataTemplate x:Key="BucketDashboardItemTemplate">
            <Grid HorizontalAlignment="Left" Width="250" Height="549.718" Background="#FF5687A6">
                <Grid>
            		<ListBox HorizontalAlignment="Left" VerticalAlignment="Top" Height="449.723" ItemsSource="{Binding Tasks}" Style="{StaticResource DashboardTaskListboxTemplate}" ItemTemplate="{StaticResource DashboardTaskListItemTemplate}" Background="#CC5687A6" Margin="0,100.277,0,-0.277" Foreground="White"/>
            		<Rectangle HorizontalAlignment="Left" Height="90.333" Stroke="#C4D9B36C" VerticalAlignment="Top" Width="250" Margin="0,459.944,0,-0.277">
            			<Rectangle.Fill>
            				<LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
            					<GradientStop Color="#005687A6"/>
            					<GradientStop Color="#CC5687A6" Offset="1"/>
            					<GradientStop Color="#DD5687A6" Offset="0.716"/>
            					<GradientStop Color="#AD5687A6" Offset="0.401"/>
            				</LinearGradientBrush>
            			</Rectangle.Fill>
            		</Rectangle>
            	</Grid>
            	<Grid Margin="0,0,0,445" Background="#CC5687A6">
            		<Grid Background="{StaticResource ListViewItemOverlayBackgroundThemeBrush}" Height="104.775" VerticalAlignment="Bottom" >
            			<Grid.RowDefinitions>
            				<RowDefinition Height="Auto"/>
            				<RowDefinition/>
            			</Grid.RowDefinitions>
            			<TextBlock Text="{Binding Description}" Foreground="{StaticResource ListViewItemOverlayForegroundThemeBrush}" Style="{StaticResource TitleTextStyle}" Margin="12,3,30,0" HorizontalAlignment="Left" FontSize="26.667" FontFamily="Segoe UI Semibold" LineHeight="32"/>
            			<TextBlock HorizontalAlignment="Right" TextWrapping="Wrap" Text="{Binding Tasks.Count}" FontSize="96" Grid.RowSpan="2" Margin="0,0,6,6" VerticalAlignment="Bottom" Height="99" FontFamily="Segoe UI Semilight" Visibility="{Binding Tasks.Count, Converter={StaticResource IntToVisibilityConverter}}"/>
            		</Grid>
            	</Grid>
            </Grid>
        </DataTemplate>
    </common:LayoutAwarePage.Resources>

    <!--
        This grid acts as a root panel for the page that defines two rows:
        * Row 0 contains the back button and page title
        * Row 1 contains the rest of the page layout
    -->
    <Grid Style="{StaticResource LayoutRootStyle}" Background="#EEDECB72">
        <Grid.RowDefinitions>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <!-- Back button and page title -->

        <!-- Horizontal scrolling grid used in most view states -->
        <GridView
            x:Name="itemGridView"
            AutomationProperties.AutomationId="ItemGridView"
            AutomationProperties.Name="Grouped Items"
            Margin="0,90,0,0"
            Padding="116,0,40,46"
            ItemsSource="{Binding Source={StaticResource groupedItemsViewSource}}"
            ItemTemplate="{StaticResource BucketDashboardItemTemplate}"
            SelectionMode="None"
            IsItemClickEnabled="True"
            ItemClick="ItemViewItemClick">

            <GridView.ItemsPanel>
                <ItemsPanelTemplate>                        
                    <VirtualizingStackPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </GridView.ItemsPanel>
            
        </GridView>

        <!-- Vertical scrolling list only used when snapped -->
        <ListView
            x:Name="itemListView"
            AutomationProperties.AutomationId="ItemListView"
            AutomationProperties.Name="Grouped Items"
            Visibility="Collapsed"
            Margin="0,-10,0,0"
            Padding="10,0,0,60"
            ItemsSource="{Binding Source={StaticResource groupedItemsViewSource}}"
            ItemTemplate="{StaticResource Standard80ItemTemplate}"
            SelectionMode="None"
            IsItemClickEnabled="True"
            ItemClick="ItemViewItemClick"/>

        <VisualStateManager.VisualStateGroups>

            <!-- Visual states reflect the application's view state -->
            <VisualStateGroup x:Name="ApplicationViewStates">
                <VisualState x:Name="FullScreenLandscape"/>
                <VisualState x:Name="Filled"/>

                <!-- The entire page respects the narrower 100-pixel margin convention for portrait -->
                <VisualState x:Name="FullScreenPortrait">
                    <Storyboard>

                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="itemGridView" Storyboard.TargetProperty="Padding">
                            <DiscreteObjectKeyFrame KeyTime="0" Value="96,0,10,56"/>
                        </ObjectAnimationUsingKeyFrames>
                    </Storyboard>
                </VisualState>

                <!--
                    The back button and title have different styles when snapped, and the list representation is substituted
                    for the grid displayed in all other view states
                -->
                <VisualState x:Name="Snapped">
                    <Storyboard>

                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="itemListView" Storyboard.TargetProperty="Visibility">
                            <DiscreteObjectKeyFrame KeyTime="0" Value="Visible"/>
                        </ObjectAnimationUsingKeyFrames>
                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="itemGridView" Storyboard.TargetProperty="Visibility">
                            <DiscreteObjectKeyFrame KeyTime="0" Value="Collapsed"/>
                        </ObjectAnimationUsingKeyFrames>
                    </Storyboard>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
    </Grid>
</common:LayoutAwarePage>
